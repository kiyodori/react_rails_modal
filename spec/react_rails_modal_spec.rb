require "spec_helper"

describe ReactRailsModal do
  it "has a version number" do
    expect(ReactRailsModal::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
